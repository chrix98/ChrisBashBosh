#!/bin/bash

##//	DEV01A
remote_host[${#remote_host[*]}]="lb01.cloud"
remote_user[${#remote_user[*]}]="administrator"
remote_port[${#remote_port[*]}]="22"

##//	DEV01B
remote_host[${#remote_host[*]}]="lb01a.cloud"
remote_user[${#remote_user[*]}]="administrator"
remote_port[${#remote_port[*]}]="22"

##//	DEV01B
remote_host[${#remote_host[*]}]="lb01b.cloud"
remote_user[${#remote_user[*]}]="administrator"
remote_port[${#remote_port[*]}]="22"
